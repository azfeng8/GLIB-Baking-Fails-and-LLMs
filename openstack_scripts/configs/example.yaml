# Example config for launching OpenStack experiments.
---
# This specifies the curiosity method argument.
APPROACHES:
  glib_l2_warmstartLNDR:
    NAME: "GLIB_L2"
    FLAGS:
      learning_name: LLMWarmStart+LNDR
# Environments to run on.
ENVS:
  baking:
    NAME: "Baking"
ARGS: []
FLAGS: {}
START_SEED: 1
NUM_SEEDS: 1
